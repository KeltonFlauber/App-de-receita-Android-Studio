<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.ReceitasRapidas" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/darkOrange</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/darkOrange</item>
        <!-- Customize your theme here. -->
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="SimpleSearchViewStyle">
        <!-- Change search style -->
        <item name="type">card</item>

        <!-- Change search hint -->
        <item name="android:hint">Search</item>

        <!-- Change search inputType -->
        <item name="android:inputType">text</item>

        <!-- Change search textColor -->
        <item name="android:textColor">@color/white</item>

        <!-- Search bar/card background -->
        <item name="searchBackground">@color/darkOrange</item>

        <!-- Change icons -->
        <item name="searchBackIcon">@drawable/ic_baseline_arrow_back_24</item>

        <!-- Change icons tint -->
        <item name="iconsTint">@color/white</item>
        <item name="backIconTint">@color/white</item>

        <!-- Change icons alpha -->
        <item name="backIconAlpha">0.8</item>
        <item name="iconsAlpha">0.8</item>

        <!-- Change search input colors -->
        <item name="hintColor">@color/default_textColorHint</item>

    </style>
</resources>