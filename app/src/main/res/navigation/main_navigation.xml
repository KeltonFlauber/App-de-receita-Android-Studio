<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main_navigation"
    app:startDestination="@id/mainScreen">

    <fragment
        android:id="@+id/mainScreen"
        tools:layout="@layout/frag_main"
        android:label="Home"
        android:name="com.example.receitasrapidas.fragments.MainScreen">
        <action
            android:id="@+id/action_mainScreen_to_favorites"
            app:destination="@id/favorites" />
    </fragment>
    <fragment
        android:id="@+id/favorites"
        tools:layout="@layout/favorites_frag"
        android:name="com.example.receitasrapidas.fragments.Favorites"
        android:label="Favourites" />
</navigation>